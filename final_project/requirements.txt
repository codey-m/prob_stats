# Higgs final project — tested with these (newer minor versions are generally fine)
numpy>=1.24,<3
pandas>=2.0,<3
scipy>=1.10,<2
scikit-learn>=1.3,<2
matplotlib>=3.7,<4
nbformat>=5.7          # only needed to rebuild the notebook from build_notebook.py
